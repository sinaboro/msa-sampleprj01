server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sinaboro/msa-sampleprj01.git
          clone-on-start: true
          default-label: main
          # 만약 private repo일 경우에는
          # username: github사용자명
          # password: 토큰값